# DarkMesh Hardware Image

Place the Raspberry Pi / Jetson bench photo here as:

```text
assets/hardware/darkmesh-pis.jpg
```

The public site already references that path as the remote-node hardware image. Keep the photo free of credentials, personal screens, private Foundry URLs, private messages, and readable passwords.
